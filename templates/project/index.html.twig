{% extends 'base.html.twig' %}

{% block body %}
    <h1>All Projects</h1>
    <ul>
        {% for project in projects %}
            <li>{{ project.projectName }} - <a href="{{ path('project_show', {id: project.projectid}) }}">View</a></li>
        {% endfor %}
    </ul>
{% endblock %}